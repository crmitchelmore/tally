name: Redeploy Production (Vercel)

on:
  workflow_dispatch:

env:
  NX_DAEMON: false

jobs:
  redeploy-prod-vercel:
    name: Redeploy Vercel (prod)
    runs-on: ubuntu-latest
    environment: production
    # Run from repo root; Vercel project Root Directory is already set to "tally-web"
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 20

      - name: Redeploy to Vercel (prod)
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
        working-directory: tally-web
        run: |
          npm i -g vercel@latest
          mkdir -p .vercel
          echo '{"projectId":"prj_tXdIJDmRB1qKZyo5Ngat62XoaMgw","orgId":"team_ifle7fkp7usKufCL8MUCY1As"}' > .vercel/project.json
          vercel deploy --prod --yes --token "$VERCEL_TOKEN" \
            --build-env SENTRY_AUTH_TOKEN="$SENTRY_AUTH_TOKEN" \
            --build-env SENTRY_ENVIRONMENT="production" \
            --env SENTRY_ENVIRONMENT="production"
