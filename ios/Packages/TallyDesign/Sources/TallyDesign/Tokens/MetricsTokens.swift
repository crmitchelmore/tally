import SwiftUI

/// Layout metrics for consistent UI sizing
public enum TallyMetrics {
    /// Minimum touch target size based on Apple's Human Interface Guidelines.
    public static let minTouchTarget: CGFloat = 44
}
