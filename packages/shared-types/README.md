# @tally/shared-types

Shared TypeScript types that mirror the Convex schema (for web + mobile).

## JSON Schema

A JSON Schema is also provided for non-TypeScript clients:

- `schema.json` (definitions under `$defs`, e.g. `Challenge`, `Entry`)
