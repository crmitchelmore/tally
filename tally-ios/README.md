# Tally iOS

This directory will hold the native iOS app.

## Current status

- `TallyCore/` is a Swift Package containing shared models + a lightweight HTTP client for the Convex HTTP API.

## API base URL

Convex HTTP routes are served from:

- `https://<deployment>.convex.site`

Example (prod): `https://bright-jackal-396.convex.site`
