// CI-safe Release configuration
// This file is checked into git with placeholder/empty values.
// For local development, override via Release.local.xcconfig (gitignored)

// Clerk publishable key - set via environment variable or Release.local.xcconfig
CLERK_PUBLISHABLE_KEY_PROD =

// LaunchDarkly mobile key - set via environment variable or Release.local.xcconfig
LAUNCHDARKLY_MOBILE_KEY_PROD =

// Include local overrides if they exist (gitignored)
#include? "Release.local.xcconfig"
