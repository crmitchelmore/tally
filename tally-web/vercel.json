{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "cd .. && bun install --frozen-lockfile",
  "buildCommand": "cd .. && (cd packages/shared-types && bun run build) && (cd tally-web && bun run build)",
  "outputDirectory": ".next"
}
